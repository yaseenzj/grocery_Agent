# ─────────────────────────────────────────────────────────────────────────────
# Grocery Lifecycle Tracker — Python Dependencies
# ─────────────────────────────────────────────────────────────────────────────
# Install with:  pip install -r requirements.txt
# Python:        >= 3.11

# ── Web Framework ─────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0          # ASGI server with websocket support

# ── Data Validation ───────────────────────────────────────────────────────────
pydantic>=2.7.0,<3.0.0             # Runtime schema validation (v2 required)

# ── LangChain Core & Google Integration ───────────────────────────────────────
langchain>=0.2.0
langchain-core>=0.2.0
langchain-google-genai>=4.2.6      # ChatGoogleGenerativeAI + Gemini bindings (gemini-1.5-pro)

# ── Environment & Config ──────────────────────────────────────────────────────
python-dotenv>=1.0.0               # Load GOOGLE_API_KEY from .env at startup

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dateutil>=2.9.0             # Robust date parsing for expiration engine
